[pytest]
# Adiciona a raiz do projeto ao sys.path, para que "from src.analise_riscos import ..."
# funcione ao rodar `pytest` de qualquer lugar, sem precisar instalar o projeto como pacote.
pythonpath = .
testpaths = tests
